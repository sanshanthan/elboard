import 'package:flutter/material.dart';

class MarketScreen extends StatelessWidget {
  final Color color;
  MarketScreen(this.color);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      color: color,
    );
  }
}
